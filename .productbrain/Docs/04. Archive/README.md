# 04. Archive

Completed projects and inactive areas/resources. Move folders here when done — do not delete (history matters).

When archiving, ensure outcomes are captured on the Chain (`INS`, `DEC`, promoted `WP` status).
