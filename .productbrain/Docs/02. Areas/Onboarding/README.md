# Onboarding

Working docs for getting started with FunCode — **not Chain SSOT**. Durable specs live on the Chain; retrieve with `pb get`.

Every guide here follows **`STD-5`** (onboarding guide contract) and the atom decisions **`DEC-17`–`DEC-21`**: zero-context explainers, human + agent optimized, always framed as harness + Product Brain + Play Room, for team and community alike.

| Doc | For |
|-----|-----|
| **[Your first Play Room in 15 minutes](./first-play-room-in-15-minutes.md)** | Fastest path — connect, fork, orient, small change, lint, PR, capture. Start here. |
| **[Cursor, your harness — and Product Brain](./cursor-the-harness-and-product-brain.md)** | The tool itself — install, Tab/inline/Agent, modes, context & rules, models & cost, the `pb` loop, team management. Zero Cursor experience needed. |
| **[Design system + harness + Product Brain](./design-system-with-harness-and-productbrain.md)** | UI depth — tokens/atoms, `pb` workflow, Play Room loop. **Feed this doc back into your agent.** |

```bash
pb get STD-5   # onboarding guide contract
pb get WP-1    # root bet — why we're here
pb get FLO-1   # member onboarding flow
pb get DEC-17  # agent-feedable primer decision
```
