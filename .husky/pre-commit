#!/usr/bin/env sh
# STD-10 — file-size guard + design-system lint on staged web/ changes
set -e

node web/scripts/check-file-size.mjs

# lint:ds only when staged files touch web UI
if git diff --cached --name-only | grep -q '^web/'; then
  (cd web && npm run lint:ds)
fi
